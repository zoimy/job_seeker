# Чеклист для Отладки в Браузере

Так как backend работает и ENV vars правильные, проблема в том как frontend загружает данные.

## 📋 Что проверить в браузере

### 1. Откройте сайт с DevTools

1. Откройте https://job-seeker-dusky.vercel.app
2. Нажмите **F12** (откроется DevTools)
3. **Обновите страницу** (F5 или Cmd+R)

---

### 2. Проверьте вкладку Network

1. Перейдите на вкладку **Network**
2. Обновите страницу (чтобы записать все запросы)
3. Найдите запрос к `/api/profile`
4. Проверьте:

**Request URL должен быть:**
```
https://job-seeker-p8zi.onrender.com/api/profile
```

❌ **НЕ должно быть:**
```
http://localhost:5001/api/profile
```

**Request Headers должны включать:**
```
x-user-id: 3383eb45-6c04-4181-9504-07c6018b0c6b
```

**Response Headers должны включать:**
```
access-control-allow-origin: https://job-seeker-dusky.vercel.app
```

**Response Status:**
```
200 OK
```

**Response Body должен содержать:**
```json
{
  "profile": {
    "name": "Egor",
    "role": "Frontend Developer",
    ...
  }
}
```

---

### 3. Проверьте вкладку Console

Поищите в Console любые ошибки:

**Плохие признаки:**
- ❌ `CORS error`
- ❌ `Failed to fetch`
- ❌ `Network Error`
- ❌ `TypeError`
- ❌ Любые красные ошибки

**Хорошие признаки:**
- ✅ `✅ Profile loaded from backend: Egor`
- ✅ `🔄 Initializing app and loading profile...`

---

### 4. Проверьте localStorage

В Console введите:

```javascript
localStorage.getItem('job_tracker_user_id')
```

Должно вернуть:
```
"3383eb45-6c04-4181-9504-07c6018b0c6b"
```

Также проверьте backend URL:

```javascript
import.meta.env.VITE_BACKEND_URL
```

Или просто проверьте что profile пустой:

```javascript
JSON.parse(localStorage.getItem('user_profile') || '{}')
```

---

## 🎯 Что делать с результатами

### Если Request URL = localhost:5001
**Проблема:** Frontend не использует ENV переменную

**Решение:** 
- Проверить что переменная называется именно `VITE_BACKEND_URL` (с префиксом VITE_)
- Проверить что deployment был сделан ПОСЛЕ добавления переменной
- Сделать Redeploy на Vercel

---

### Если Status = 404 или 500
**Проблема:** Backend endpoint недоступен

**Решение:**
- Проверить что backend не спит (открыть /health)
- Проверить логи на Render

---

### Если Response Body = {"profile": null}
**Проблема:** User ID не совпадает или профиль удалён

**Решение:**
- Проверить что Request Headers содержат правильный x-user-id
- Пройти onboarding заново

---

### Если все запросы правильные но профиль пустой
**Проблема:** Race condition или ошибка в App.tsx

**Решение:**
- Посмотреть Console на ошибки
- Проверить что `useEffect` в App.tsx срабатывает
- Добавить `console.log` в storageService.getUserProfile

---

## 📸 Скриншоты для отправки мне

Если не понятно в чём проблема, сделайте скриншоты:

1. Network tab → запрос `/api/profile` (Request Headers + Response)
2. Console tab (все сообщения)
3. localStorage в Console (результат команд выше)

И отправьте мне - я помогу разобраться!
