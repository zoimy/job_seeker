# ⚠️ ВАЖНО: Как работает система поиска вакансий

## 🔍 Текущая реализация

**У вас НЕТ реального парсера с сайта rabota.md!**

### Как приложение работает сейчас:

```
┌─────────────────────────────────────────────┐
│ Пользователь нажимает "AI Scan"             │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
         ┌─────────────────┐
         │ Есть API ключ?  │
         └────┬───────┬────┘
              │       │
        ДА ◄──┘       └──► НЕТ
         │                 │
         ▼                 ▼
┌─────────────────┐  ┌─────────────────┐
│ Google Gemini   │  │ Mock данные     │
│ генерирует      │  │ (2 тестовые     │
│ вакансии через  │  │ вакансии)       │
│ AI              │  │                 │
└─────────────────┘  └─────────────────┘
```

### Что происходит сейчас:

1. **С API ключом Gemini:**
   - AI получает ваш профиль (навыки, опыт, локация, зарплата)
   - AI **ПРИДУМЫВАЕТ** 4 вакансии на основе вашего профиля
   - Данные **НЕ реальные**, это генерация AI

2. **Без API ключа:**
   - Показываются 2 захардкоженные тестовые вакансии
   - Данные полностью статичные

### ❌ Что НЕ делается:

- ❌ НЕТ парсинга rabota.md
- ❌ НЕТ парсинга других сайтов с вакансиями
- ❌ НЕТ реальных данных о вакансиях
- ❌ НЕТ скрейпинга веб-страниц

---

## ✅ Что изменено (24 часа)

Теперь **по умолчанию** система показывает вакансии за **последние 24 часа**.

### Доступные опции в Profile:
- ✨ **Last 24 hours** (по умолчанию)
- Last 7 days
- Last 14 days  
- Last 30 days

---

## 🚀 Как сделать РЕАЛЬНЫЙ парсинг rabota.md

Для реального парсинга нужно:

### Вариант 1: Backend + Web Scraping

```typescript
// backend/scrapers/rabotaMdScraper.ts
import axios from 'axios';
import * as cheerio from 'cheerio';

export async function scrapeRabotaMd(params: {
  query: string;
  location: string;
  period?: number; // days
}) {
  const url = `https://www.rabota.md/ru/jobs-${params.location}-${params.query}`;
  
  const response = await axios.get(url);
  const $ = cheerio.load(response.data);
  
  const vacancies = [];
  
  // Парсим каждую вакансию
  $('.vacancy-item').each((i, el) => {
    const title = $(el).find('.vacancy-title').text().trim();
    const company = $(el).find('.company-name').text().trim();
    const salary = $(el).find('.salary').text().trim();
    const postedDate = $(el).find('.posted-date').text().trim();
    
    vacancies.push({
      title,
      company,
      salary,
      postedDate,
      url: $(el).find('a').attr('href')
    });
  });
  
  return vacancies;
}
```

**Проблемы:**
- ⚠️ Может нарушать Terms of Service rabota.md
- ⚠️ Сайт может банить за слишком частые запросы
- ⚠️ Структура HTML может измениться
- ⚠️ Нужен backend сервер (Node.js/Python)

---

### Вариант 2: API rabota.md (если есть)

Проверьте есть ли у rabota.md официальное API:
- Свяжитесь с rabota.md
- Запросите API доступ
- Используйте официальные endpoints

**Преимущества:**
- ✅ Легально
- ✅ Стабильно
- ✅ Данные структурированы

---

### Вариант 3: RSS Feed (если доступен)

Некоторые job boards предоставляют RSS:
```typescript
import Parser from 'rss-parser';

const parser = new Parser();
const feed = await parser.parseURL('https://rabota.md/rss/...');

feed.items.forEach(item => {
  console.log(item.title, item.link, item.pubDate);
});
```

---

### Вариант 4: Использовать существующие API

Альтернативные источники вакансий:

1. **LinkedIn Jobs API**
2. **Indeed API**
3. **Google Jobs API**
4. **Adzuna API** (бесплатный tier)
5. **Reed.co.uk API**

---

## 🔧 Что нужно добавить в проект

### 1. Backend сервис

```bash
# Создать папку backend
mkdir backend
cd backend
npm init -y
npm install express axios cheerio cors
```

### 2. API Route для парсинга

```typescript
// backend/routes/scrape.ts
import express from 'express';
import { scrapeRabotaMd } from '../scrapers/rabotaMdScraper';

const router = express.Router();

router.post('/scrape/rabota-md', async (req, res) => {
  try {
    const { query, location, searchPeriodDays } = req.body;
    
    const vacancies = await scrapeRabotaMd({
      query,
      location,
      period: searchPeriodDays
    });
    
    res.json({ success: true, vacancies });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

export default router;
```

### 3. Обновить Frontend

```typescript
// services/rabotaMdService.ts
export async function fetchRealVacancies(profile: UserProfile) {
  const response = await fetch('http://localhost:5000/api/scrape/rabota-md', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      query: profile.role,
      location: profile.location,
      searchPeriodDays: profile.searchPeriodDays
    })
  });
  
  const data = await response.json();
  return data.vacancies;
}
```

---

## 📋 План реализации реального парсера

### Этап 1: Исследование (1-2 часа)
- [ ] Изучить структуру HTML rabota.md
- [ ] Проверить наличие официального API
- [ ] Проверить robots.txt и Terms of Service
- [ ] Определить селекторы для данных

### Этап 2: Backend (2-3 часа)
- [ ] Создать Express сервер
- [ ] Реализовать scraper с cheerio/puppeteer
- [ ] Добавить кеширование (Redis/In-memory)
- [ ] Добавить rate limiting
- [ ] Обработка ошибок

### Этап 3: Frontend (1 час)
- [ ] Создать сервис для запросов к backend
- [ ] Обновить `generateSimulatedMatches` для использования реальных данных
- [ ] Добавить индикатор "Real data" vs "AI generated"

### Этап 4: Тестирование (1 час)
- [ ] Проверить разные поисковые запросы
- [ ] Проверить фильтрацию по дате
- [ ] Проверить обработку ошибок

---

## 🎯 Рекомендации

### Краткосрочно (сегодня):
1. ✅ **Используйте Gemini AI** - добавьте API ключ в `.env`
2. ✅ AI будет генерировать релевантные вакансии на основе вашего профиля
3. ✅ Установлен период "24 часа" по умолчанию

### Среднесрочно (эта неделя):
1. 🔨 Свяжитесь с rabota.md для получения API доступа
2. 🔨 Или реализуйте scraper (см. код выше)
3. 🔨 Добавьте backend сервис

### Долгосрочно (следующий месяц):
1. 🚀 Интеграция с несколькими job boards
2. 🚀 ML модель для улучшения матчинга
3. 🚀 Автоматические уведомления о новых вакансиях

---

## ⚡ Быстрый старт с реальными данными

Если вы хотите **прямо сейчас** видеть реальные вакансии:

### 1. Добавьте Gemini API ключ

Откройте `.env`:
```env
GEMINI_API_KEY=ваш_ключ_сюда
```

Получите ключ: https://aistudio.google.com/app/apikey

### 2. Обновите промпт для более точных результатов

AI будет генерировать вакансии максимально близкие к реальным данным с rabota.md.

---

## 📞 Нужна помощь?

Если вы хотите реализовать реальный парсер, дайте знать и я:
1. Помогу изучить структуру rabota.md
2. Создам полный backend scraper
3. Интегрирую его с вашим frontend

**Но помните:** Скрейпинг может нарушать правила сайта. Лучше всего - использовать официальное API!
