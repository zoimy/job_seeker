# 🔐 Настройка переменных окружения

## Созданные файлы

✅ **`.env`** - Файл с вашими приватными ключами (НЕ коммитить в git)  
✅ **`.env.example`** - Шаблон переменных (можно коммитить в git)  
✅ **`.gitignore`** - Обновлён для игнорирования `.env` файлов

---

## 📋 Как настроить API ключ Gemini

### Шаг 1: Получите API ключ

1. Откройте 👉 **[Google AI Studio](https://aistudio.google.com/app/apikey)**
2. Войдите в свой Google аккаунт
3. Нажмите **"Get API key"** или **"Create API key"**
4. Скопируйте ключ в буфер обмена

### Шаг 2: Добавьте ключ в `.env`

Откройте файл `.env` в корне проекта и замените:

```env
GEMINI_API_KEY=
```

на ваш реальный ключ:

```env
GEMINI_API_KEY=AIzaSyAaBbCcDdEeFfGgHhIiJjKkLlMmNnOoPpQq
```

### Шаг 3: Перезапустите сервер

После изменения `.env` файла, перезапустите dev сервер:

```bash
# Остановите текущий сервер (Ctrl + C)
# Затем запустите снова
npm run dev
```

---

## 🔍 Проверка переменных

В файле `vite.config.ts` переменные подключены так:

```typescript
define: {
  'process.env.API_KEY': JSON.stringify(env.GEMINI_API_KEY),
  'process.env.GEMINI_API_KEY': JSON.stringify(env.GEMINI_API_KEY)
}
```

В `geminiService.ts` используется:

```typescript
if (!process.env.API_KEY) {
  console.warn("No API Key provided, returning mock data");
  return generateMockMatches();
}
```

---

## 📝 Доступные переменные

| Переменная | Описание | Обязательна? |
|------------|----------|--------------|
| `GEMINI_API_KEY` | API ключ Google Gemini | ❌ Нет* |
| `VITE_APP_NAME` | Название приложения | ❌ Нет |
| `VITE_APP_PORT` | Порт dev сервера | ❌ Нет |

*Без API ключа приложение работает с mock данными (тестовые вакансии).

---

## ⚠️ Важно!

- **НЕ коммитьте** файл `.env` в git! Он уже добавлен в `.gitignore`
- **НЕ делитесь** вашим API ключом публично
- `.env.example` можно коммитить - он не содержит приватных ключей
- При деплое на продакшн добавьте переменные окружения в настройки хостинга

---

## 🚀 Результат

После добавления ключа:
- ✅ AI Scan будет генерировать реальные вакансии через Gemini AI
- ✅ Совпадения будут более точными и релевантными
- ✅ Можно использовать все возможности AI

Без ключа:
- ⚠️ Приложение работает с 2 mock вакансиями
- ⚠️ Данные статичные, без AI обработки
